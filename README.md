# 🤖 BotBudget - بوت إدارة المصاريف الشخصية

بوت تيليجرام شخصي لتتبع المصاريف والدخل بذكاء اصطناعي (Google Gemini 2.5 Flash).

## ✨ الميزات
- 📝 تسجيل المصاريف والدخل بالكتابة الطبيعية بالعربية
- 🔁 إدارة المدفوعات المتكررة مع تنبيهات يومية
- 📊 تقارير يومية وشهرية
- 📤 تصدير البيانات بصيغة CSV و Excel
- 🔒 حماية بـ Whitelist + Rate Limiting

## 📁 هيكل المشروع
```
BotBudget/
├── main.py              # نقطة الدخول الرئيسية
├── config.py            # إعدادات التطبيق
├── .env                 # المتغيرات السرية
├── requirements.txt     # المكتبات المطلوبة
├── ai/                  # وحدة الذكاء الاصطناعي (Gemini)
├── db/                  # طبقة قاعدة البيانات
├── models/              # النماذج (Domain Models)
├── repositories/        # طبقة الوصول للبيانات
├── services/            # طبقة منطق التطبيق
├── handlers/            # طبقة التعامل مع تيليجرام
├── security/            # طبقة الحماية
└── utils/               # أدوات مساعدة
```

## 🚀 التشغيل
```bash
# 1. تثبيت المكتبات
pip install -r requirements.txt

# 2. إعداد ملف .env بالبيانات المطلوبة

# 3. تشغيل البوت
python main.py
```

## 🔒 التأمين
أضف رقم حسابك (Telegram ID) في ملف `.env`:
```
ALLOWED_USER_IDS=123456789
```
استخدم أمر `/myid` في البوت لمعرفة رقمك.
