services:
  bot:
    build: .
    container_name: botbudget
    restart: unless-stopped
    env_file:
      - .env
    network_mode: host
    volumes:
      - ./exports:/app/exports
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"

