# 🧠 AI Module

وحدة الذكاء الاصطناعي المسؤولة عن تحليل النصوص العربية باستخدام **Google Gemini 2.5 Flash**.

## الملفات
- `gemini_parser.py` - المحلل الذكي للنصوص المالية

## الوظائف الرئيسية
| الدالة | الوصف |
|---|---|
| `parse_transaction(text)` | تحويل نص عربي لبيانات مصروف/دخل (JSON) |
| `parse_recurring(text)` | تحويل نص عربي لبيانات دفعة متكررة (JSON) |

## مثال
```python
from ai.gemini_parser import parse_transaction
result = parse_transaction("صرفت ٥٠ يورو أكل")
# {'type': 'expense', 'amount': 50, 'category': 'طعام', 'date': '2026-02-21'}
```
